

import SwiftUI

struct ReloadingView: View {
    var body: some View {
        ZStack {
                    
                    VStack {
                        VStack {
                            ProgressView()
                            Text("Reloading Data")
                        }
                    }
                }

    }
}
